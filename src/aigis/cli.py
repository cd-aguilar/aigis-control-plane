"""AIGIS Control Plane — CLI entry point.

Fase 2+ (pendiente): wire up TaskContract loading + orquestación del loop
Agent Runtime -> Sandbox -> Quality Gates -> Evidence -> Decision Engine.
Fase 0/1 (este commit) solo define el domain layer; no hay runtime todavia.
"""


def main() -> None:
    raise NotImplementedError(
        "CLI aun no implementado - ver docs/ARCHITECTURE.md seccion 24 (Estado actual)"
    )


if __name__ == "__main__":
    main()
